apiVersion: v1
kind: Service
metadata:
  name: growthos-web
  labels:
    app.kubernetes.io/name: growthos-web
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: growthos-web
  ports:
    - name: http
      port: 80
      targetPort: http
