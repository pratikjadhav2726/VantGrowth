apiVersion: v1
kind: Service
metadata:
  name: growthos-api
  labels:
    app.kubernetes.io/name: growthos-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: growthos-api
  ports:
    - name: http
      port: 80
      targetPort: http
