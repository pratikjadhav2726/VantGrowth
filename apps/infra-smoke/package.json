{
  "name": "@growthos/infra-smoke",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "smoke": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@growthos/core": "workspace:^",
    "@growthos/db": "workspace:*",
    "@growthos/worker-outbox-publisher": "workspace:*",
    "nats": "^2.29.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "vitest": "^2.1.8"
  }
}
